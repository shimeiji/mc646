package mc646;

import static org.junit.Assert.*;


public class recentFiles {
	
	@Test
	public void showRecentFiles() {
		File file = new File();
		FileList recentList = new FileList();
		
		recentList.addfile(file);
		assertNull(recenList);
		
	}

}
